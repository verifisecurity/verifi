# verifi-cli

Shippable product surface: CLI + UI, cross-platform; wraps all backend tools (enterprise
install, or OSS the CLI and maintain the backend behind it). Runtime/language TBD
(single-binary candidates: Go/Rust).

See ancestor `~/Desktop/verifi/CLAUDE.md` and Confluence space VS (Architecture/Requirements).
